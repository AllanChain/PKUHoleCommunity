on:
  push:
    tags: 
      - 'v*'

env:
  NODE_VERSION: '12.x'

jobs:
  build-and-deploy:
    name: Build and Release
    runs-on: ubuntu-latest
    # Disable pre release builds before #34 resolves
    if: ${{ ! contains(github.ref, '-') }}
    steps:
    - uses: actions/checkout@v2
      with:
        submodules: true
    - name: Use Node.js ${{ env.NODE_VERSION }}
      uses: actions/setup-node@v1
      with:
        node-version: ${{ env.NODE_VERSION }}
    - name: Install Packages
      run: npm install
    - name: Build App
      run: npm run build
      env:
        # React will fail CI if there is any warning
        CI: false
    # Yes, previous beta assets are still available
    # but that does not matter too much.
    # It will be clean after stable release
    - name: Prepare Latest Stable Version
      if: ${{ contains(github.ref, '-') }}
      uses: actions/checkout@v2
      with:
        submodules: true
        path: dist
        ref: 'gh-pages'
    - name: Merge Stable and Beta Version
      if: ${{ contains(github.ref, '-') }}
      run: |
        mv build/index.html build/beta.html
        cp -r build/* dist
    - name: Copy as Beta
      if: ${{ ! contains(github.ref, '-') }}
      run: |
        cp -r build dist
        cp dist/index.html dist/beta.html
    - name: Deploy
      uses: peaceiris/actions-gh-pages@v3
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        publish_dir: ./dist
        publish_branch: gh-pages
